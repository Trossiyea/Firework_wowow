**角色设定**：
你现在是一位精通现代前端技术（HTML5/Canvas/WebGL）和计算机视觉交互的资深全栈工程师，同时具备极高的UI/UX设计品味（精通Apple式的极简美学）。

**任务目标**：
请为我编写一个基于Web的“2026马年手势控制烟花模拟器”的完整前端项目框架（包含HTML结构、CSS样式和核心JavaScript逻辑）。

**整体风格（UI/UX）**：

* **视觉语言**：采用类似于Apple的深色模式（Dark Mode）极简设计。背景以纯净的深邃夜空为主，UI组件使用毛玻璃效果（Glassmorphism，半透明背景+细微的高斯模糊+微小的白色边框），确保不喧宾夺主。
* **动画过渡**：所有页面的切换、面板的展开收起、滑块的拖动均需具备平滑的阻尼感和呼吸感（Easing Animations）。

**核心功能与交互流程**：

**1. 初始授权与开场界面**

* **视觉呈现**：页面全屏显示深色渐变背景，屏幕正中央以优雅的非衬线字体（如San Francisco或PingFang SC）浮现带有呼吸灯效果的金色祝福语：“2026，金马逢春”。
* **交互逻辑**：页面加载完毕后，自动调用 `navigator.mediaDevices.getUserMedia` 请求用户的摄像头权限。
* **状态处理**：在用户做出选择前，页面保持静谧的等待状态；一旦用户点击“允许”，祝福语平滑淡出，背景无缝过渡到烟花模拟器主界面。

**2. 烟花模拟器主界面**

* **画布区**：占据全屏的核心交互区，用于实时渲染用户控制的烟花。
* **功能控制面板（左侧/悬浮侧边栏）**：使用毛玻璃面板悬浮于左侧，包含以下矩阵功能：
* **形状预设 (Presets)**：提供几个极简的图标按钮，允许切换烟花绽放的形状（如：经典球形、爱心、五角星、马头轮廓）。
* **自定义文案 (Text Firework)**：一个优雅的输入框，用户输入短句后，下一次释放的烟花将以该文字的形状在夜空中排布绽放。
* **规模调节 (Size Slider)**：一个带有平滑反馈的滑动条，用于控制烟花的爆炸半径和粒子数量。
* **色彩定制 (Color Picker)**：一个精致的色盘或预设颜色组（如中国红、琉璃金、青金石等），用于改变烟花的主色系。



**3. 手势控制逻辑 (核心)**

* **技术选型**：引入轻量级的前端手势识别库（如 MediaPipe Handpose 或 TensorFlow.js）。
* **映射规则**：利用前置摄像头实时捕捉用户手部动作。例如：食指指尖的坐标映射为烟花发射的准星（屏幕上显示一个微弱的光标）；当用户做出“握拳”或“打响指”的特定动作时，触发烟花在当前准星位置发射并爆炸。

**技术要求**：

* 请使用纯原生HTML/CSS/JS构建基础框架，烟花粒子效果请使用 Canvas API 编写物理逻辑（重力、空气阻力、粒子消散）。
* 代码需具备高可读性和模块化，关键逻辑请用中文添加详细注释。
